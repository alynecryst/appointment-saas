version: "3"

services:

  mongodb:
    image: mongo:7
    container_name: appointment-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: appointment
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: